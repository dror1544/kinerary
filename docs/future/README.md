# Future

Concepts that are **not built and not in flight**: proposals, target
architectures and position papers kept so the thinking is not lost.

Nothing in this directory describes current behaviour. A plan here may name
files, endpoints or hosts that never existed or have since changed.
**Check the tree before believing any of it.**

When work on one of these starts, its document moves back to `docs/` and gets
a status line.
